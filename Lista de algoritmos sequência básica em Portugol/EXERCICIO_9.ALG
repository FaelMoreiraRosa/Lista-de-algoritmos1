Algoritmo "Exercicio_9"

var
altura, largura, area, tinta:real
inicio
   
   escreval("Digite a altura da parede:")
   leia(altura)
   
   escreval("Digite a largura da parede:")
   leia(largura)

  area <- altura * largura
   
   escreval("Você pode obter ", result, " dólares com essa quantia de reais")

Fimalgoritmo